{
  "entities": {
    "User": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "User",
      "type": "object",
      "description": "Represents a user of the Axiom Trade Table Clone application.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the user entity."
        },
        "email": {
          "type": "string",
          "description": "The email address of the user.",
          "format": "email"
        },
        "name": {
          "type": "string",
          "description": "The name of the user."
        },
        "profilePic": {
          "type": "string",
          "description": "URL of the user's profile picture."
        },
        "country": {
            "type": "string",
            "description": "The user's country."
        },
        "contact": {
            "type": "string",
            "description": "The user's contact number."
        }
      },
      "required": [
        "id",
        "email",
        "name"
      ]
    }
  },
  "auth": {
    "providers": [
      "password",
      "google.com"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/users/{userId}",
        "definition": {
          "entityName": "user",
          "schema": {
            "$ref": "#/backend/entities/User"
          },
          "description": "Stores user profiles. Path-based ownership ensures only the authenticated user can access their own profile data.",
          "params": [
            {
              "name": "userId",
              "description": "The unique identifier of the user, obtained from Firebase Authentication."
            }
          ]
        }
      }
    ],
    "reasoning": "This Firestore structure prioritizes authorization independence and efficient data access for the Axiom Trade Table Clone application. User data is stored under the `/users/{userId}` path, providing clear ownership. Authentication providers are password and google.com. The structure is designed to avoid hierarchical authorization dependencies and enables secure list operations. Since there is no collaborative entities, no membership maps were provided."
  }
}
